apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - secret.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

# Override the image tag for a specific release:
# images:
#   - name: ghcr.io/dusthoff/msgraphmcp
#     newTag: "1.2.3"
