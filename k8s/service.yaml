apiVersion: v1
kind: Service
metadata:
  name: msgraphmcp
  namespace: msgraphmcp
  labels:
    app.kubernetes.io/name: msgraphmcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: msgraphmcp
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
